config:
  target: "http://ec2-13-56-238-185.us-west-1.compute.amazonaws.com"
  phases:
    - duration: 10
      arrivalRate: 1000

scenarios:
  - name: "Test houses and prices api"
    flow:
      # - post:
      #     url: "/search"
      #     body: "kw={{ keywords }}"
      #     capture:
      #       json: "$.results[0].id"
      #       as: "id"
      - get:
          url: "/{{$randomNumber(9999990,9999999)}}"
      # - get:
      #     url: "/prices/{{$randomNumber(9999990,9999999)}}"

      # - think: 3
      # - post:
      #     url: "/cart"
      #     json:
      #       productId: "{{ id }}"