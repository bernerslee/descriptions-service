config:
  target: "http://ec2-54-215-233-92.us-west-1.compute.amazonaws.com:3001"
  phases:
    - duration: 10
      arrivalRate: 1000

scenarios:
  - name: "Test houses and prices api"
    flow:
      # - post:
      #     url: "/search"
      #     body: "kw={{ keywords }}"
      #     capture:
      #       json: "$.results[0].id"
      #       as: "id"
      - get:
          url: "/houses/{{$randomNumber(9999900,9999999)}}"
      - get:
          url: "/prices/{{$randomNumber(9999900,9999999)}}"

      # - think: 3
      # - post:
      #     url: "/cart"
      #     json:
      #       productId: "{{ id }}"